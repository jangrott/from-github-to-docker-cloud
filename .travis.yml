sudo: required

language: java

jdk:
  - oraclejdk8

env:
  global:
    - IMAGE=jangrott/from_github_to_docker_cloud

services:
  - docker

before_script:
  - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
  - docker build -t $IMAGE:stage .

script:
  - gradle check
  - docker run -d -p 8090:8090 $IMAGE:stage
  - docker ps | grep -q $IMAGE:stage
  - if [ "$TRAVIS_BRANCH" == "master" ]; then docker push $IMAGE:stage; fi
  - sleep 120
  - gradle acceptanceTest -Denv.host=fromgithubtodockercloud-stage.89d7c692.svc.dockerapp.io

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then docker tag $IMAGE:stage $IMAGE:latest
  - if [ "$TRAVIS_BRANCH" == "master" ]; then docker push $IMAGE:latest; fi